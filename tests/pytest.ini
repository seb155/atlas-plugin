[pytest]
testpaths = tests
markers =
    slow: marks tests as slow (deselect with -m "not slow")
    hook: marks tests that execute hook scripts via subprocess
    e2e: end-to-end plugin tests (require built dist/ artifacts)
addopts = -q

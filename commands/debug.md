# /debug — Structured debugging

Invoke Skill `atlas:systematic-debugging` with the provided arguments.
Observe → hypothesize → test → fix. Max 2 attempts, then escalate.

**Usage**: `/debug`

ARGUMENTS: $ARGUMENTS

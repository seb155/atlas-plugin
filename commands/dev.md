# /dev — Blueprint-driven development

Invoke Skill `atlas:executing-plans` with the provided arguments.
Follow the ATLAS dev pipeline: context-discovery → plan-builder → tdd → executing-plans → verification → finishing-branch.

**Usage**: `/dev feature "desc"` | `/dev bugfix "desc"` | `/dev refactor "desc"`

ARGUMENTS: $ARGUMENTS

# /verify — Quality gates verification

Invoke Skill `atlas:verification` with the provided arguments.
Run backend tests, frontend tests, type-check, lint, security scan. Evidence before assertions.

**Usage**: `/verify [--full]`

ARGUMENTS: $ARGUMENTS

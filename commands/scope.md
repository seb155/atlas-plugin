# /scope — Detect scope drift

Invoke Skill `atlas:scope-check` with the provided arguments.
Are you touching files outside the original task? Stop and verify.

**Usage**: `/scope`

ARGUMENTS: $ARGUMENTS

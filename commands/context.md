# /context — Context audit and code maps

Invoke Skill `atlas:context-discovery` with the provided arguments.
8-phase project scan + CLAUDE.md management (W3H).

**Usage**: `/context [audit|codemap|patterns|sync-plan]`

ARGUMENTS: $ARGUMENTS

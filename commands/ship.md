# /ship — Commit, push, and optionally create PR

Invoke Skill `atlas:finishing-branch` with the provided arguments.
Smart commit (conventional commits, auto-detect type), push to remote, optional PR creation.

**Usage**: `/ship [message]`

ARGUMENTS: $ARGUMENTS

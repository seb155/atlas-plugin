# /tune — Autonomous optimization loop

Invoke Skill `atlas:experiment-loop` with the provided arguments.
Karpathy-inspired autoresearch: analyze → mutate → execute → measure → decide.

**Usage**: `/tune <experiment>`

ARGUMENTS: $ARGUMENTS

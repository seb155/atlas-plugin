# /estimate — I&C estimation pipeline

Invoke Skill `atlas:engineering-ops` in estimate mode with the provided arguments.
4-agent estimation chain with HITL gates.

**Usage**: `/estimate [project]`

ARGUMENTS: $ARGUMENTS

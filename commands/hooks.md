# /hooks — Create and manage Claude Code hooks

Invoke Skill `atlas:hookify` with the provided arguments.
Pattern-based guards, YAML+regex config, auto-reload.

**Usage**: `/hooks [create|list]`

ARGUMENTS: $ARGUMENTS

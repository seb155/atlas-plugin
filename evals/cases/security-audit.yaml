skill: security-audit
version: '1.0'
type: review
structural:
  min_body_lines: 30
  required_sections:
  - Dimension
  - Score
  required_tool_mentions:
  - Read
  - Grep
  max_token_estimate: 4000
  requires_hitl_gates: true
behavioral:
  cases:
  - id: security-001
    name: OWASP security scan
    input: Run security audit focusing on OWASP Top 10.
    expected_contains:
    - injection
    - XSS
    - authentication
    - CRITICAL
    ground_truth: Check all OWASP categories, severity classification, remediation
      steps.

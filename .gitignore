# Build output — dist/ is committed for CC git-subdir plugin discovery
# Run ./build.sh domains before committing to update dist/
# dist/

# OS files
.DS_Store
tests/__pycache__/
__pycache__/
tests/__pycache__/
tests/__pycache__/

# Vendored fork's node_modules — installed on-demand by
# scripts/pre-install-skill-check.sh (lazy install when missing)
third_party/atlas-skill-lint/node_modules/
third_party/atlas-skill-lint/package-lock.json

# Git worktrees (local ephemeral)
.claude/worktrees/

# v6.0 Approved-Mode Autonomy Engine (Phase 5)
# Per-project session state + decision log — never committed
.claude/session-state.json
.claude/decisions.jsonl
.claude/skill-usage.jsonl
.session-lock

# atlas-eval runtime artifacts (W1.5) — golden datasets are committed, results are not
evals/results/

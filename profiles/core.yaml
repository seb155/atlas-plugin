tier: core
description: Shared base — session, memory, context, personal, experiential
skills:
- session-pickup
- session-retrospective
- session-spawn
- memory-dream
- atlas-workspace-setup
- context-discovery
- deep-research
- scope-check
- youtube-transcript
- knowledge-builder
- user-profiler
- morning-brief
- morning-routine
- weekly-review
- note-capture
- reminder-scheduler
- document-generator
- atlas-doctor
- atlas-location
- atlas-onboarding
- atlas-vault
- cost-analytics
- episode-create
- intuition-log
- relationship-manager
- discovery   # v5.1+: capability scanner inspector
- agent-visibility   # v5.5+: SP-AGENT-VIS subagent visibility (statusline + CLI + tmux tail)
refs:
- atlas-visual-identity
- external-tools
- model-benchmarks-2026-04
- cc-native-features
hooks:
- session-start
- experiential-capture
- pre-compact-context
- auto-learn
- cognitive-state
- morning-brief
- timestamp-injector
- prompt-intelligence
- focus-guard
- protect-plugin-cache
- validate-bash
- permission-request
- permission-denied-log
- policy-drift-detector
- cwd-changed-env
- stop-failure
- external-capabilities
# v5.6.1+: SP-AGENT-VIS subagent hooks (context-injector + result-capture were
# silently broken pre-SP-AGENT-VIS — fixed alongside output-capture + track-stop)
- subagent-context-injector   # SubagentStart
- subagent-result-capture     # SubagentStop (existing functionality)
- subagent-output-capture     # PostToolUse[matcher=Agent] (SP-AGENT-VIS Layer 1)
- subagent-track-stop         # SubagentStop (SP-AGENT-VIS Layer 3 cleanup)
- auto-tail-agent             # SubagentStart — SP-AGENT-VIS Layer 3 auto-tail tmux pane (2026-04-17)
# v5.7.0+: Model-aware context threshold (replaces hardcoded CLAUDE_AUTOCOMPACT_PCT_OVERRIDE=83)
- context-threshold-injector  # SessionStart + UserPromptSubmit — dynamic 92/83 per model
# v5.19.0+: SP-DAIMON P1 — personal profile auto-calibration
- vault-profile-auto-load     # SessionStart — reads vault/daimon/*.md → ~/.atlas/runtime/session-calibration.json
- daimon-context-injector     # SessionStart — emits <daimon-calibration> block to Claude system prompt
# v5.21.0+: SP-DAIMON P2 — keyword-aware rule injection + pattern detection
- keyword-aware-calibration   # UserPromptSubmit — matches prompt → JIT inject DAIMON rule context
- pattern-signal-dispatcher   # PostToolUse[matcher=TaskCreate|TaskUpdate] — detect risk signals → append session-signals.jsonl
# v5.7.0+: CC 2.1.x native event adoption (Phase 2 of sleepy-tumbling-hennessy)
- worktree-setup              # WorktreeCreate — log + validate new worktree
- worktree-cleanup-native     # WorktreeRemove — event-driven cleanup (replaces cleanup-worktrees Stop hook, deprecated)
- team-idle-notify            # TeammateIdle (v2.1.33) — atlas-team coordination
- task-completed-metrics      # TaskCompleted (v2.1.33) — analytics metrics
- file-change-test-trigger    # FileChanged (v2.1.83) — DISABLED by default (ATLAS_FILE_CHANGED_ENABLED=1 to enable)
- task-created-metrics        # TaskCreated (v2.1.84) — TaskCreate usage stats
- worktree-exit-safe          # ExitWorktree (v2.1.72) Phase 3 — safety net 5-option prompt
# v5.7.0+ Phase 6F-bis: Wire pre-v5.7.0 tech-debt hooks (was KNOWN_UNDECLARED_BASELINE)
- task-created-log            # TaskCreated — lightweight analytics log
- session-replay-logger       # SessionEnd — append JSONL session archive
- session-state-writer        # SessionStart/Stop — persist session state for resume
- atlas-status-writer         # Stop/UserPromptSubmit — refresh statusline cache
- context-budget-guardian     # PreCompact — warn if compaction happens too early
# v6.0+ (2026-04-18): moved from dev-addon+admin-addon to core (was duplicated) — SessionStart scope creep detector
- feature-drift-detector      # SessionStart — flag feat() commits vs FEATURES.md PLANNED/BACKLOG
agents:
- context-scanner
persona: helpful assistant
pipeline: DISCOVER → ASSIST
banner_label: Core

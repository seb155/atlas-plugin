# ATLAS Core Domain — Foundation (REQUIRED by all other domain plugins)
# Memory, session, context, profile, vault, assist, doctor
# NO inherits — standalone domain plugin
tier: domain-core
description: "Core foundation — memory, session, context, vault, assist. Required by all ATLAS plugins."

skills:
  # Master router (generated per-domain by build)
  - atlas-assist
  # System health
  - atlas-doctor
  - atlas-onboarding
  - atlas-location
  # Personal data
  - atlas-vault
  - user-profiler
  - knowledge-builder
  - note-capture
  # Session lifecycle
  - session-pickup
  - session-retrospective
  - session-spawn
  - atlas-workspace-setup
  # Memory
  - memory-dream
  # Context
  - context-discovery
  - scope-check
  # Research
  - deep-research
  - morning-brief
  - reminder-scheduler
  # Content
  - youtube-transcript
  - document-generator

hooks:
  # Session lifecycle — direct scripts
  - session-start
  - session-end-memory-cleanup
  - experiential-capture
  # Session lifecycle — run-hook.sh subcommands
  - inject-datetime
  - session-diff
  - statusline-sync
  - checkpoint-restore
  - detect-stale-plans
  - session-end-cleanup
  # Prompt processing
  - permission-request
  - prompt-intelligence
  - timestamp-injector
  - auto-learn
  - contextual-rules-injector
  # Cognitive (SP-COGNITION)
  - tone-adaptation
  - morning-brief
  # Safety
  - validate-bash
  - focus-guard
  - stop-failure
  - session-checkpoint
  # Compaction
  - pre-compact-context
  - precompact-snapshot
  - post-compact
  # Subagent orchestration
  - subagent-context-injector
  - subagent-result-capture
  # Notifications & setup
  - notification-handler
  - setup-validator

refs:
  - web-design-guidelines
  - atlas-visual-identity

agents:
  - context-scanner

persona: "personal AI assistant"
pipeline: "DISCOVER → ASSIST → REMEMBER"
banner_label: "Core"

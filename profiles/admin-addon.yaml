tier: admin-addon
description: Superuser — includes dev tools + infra, security, governance, GMS
# v6.0 SP-DEDUP Phase 1: admin-addon INHERITS dev-addon.
# The build resolves skills/refs/agents/hooks via `resolve_field` (union + dedup).
# Delta below lists ONLY admin-specific items; shared items come from dev-addon.
# 2026-04-17 — 33 skills, 13 hooks, 7 agents, 8 refs removed (inherited from dev-addon).
inherits: dev-addon
skills:
- flow-analytics
# Infrastructure & DevOps
- devops-deploy
- deploy-hotfix
- infrastructure-ops
- infrastructure-change
- statusline-setup
- infra-health
- secret-manager
# Security & Audit
- security-audit
- enterprise-audit
- codebase-audit
- code-analysis
- skill-security-audit
- agent-readiness
- product-health
# Governance & Planning
- plan-review
- programme-manager
- feature-board
- onboarding-check
- marketplace-manager
- persona-loadout
- auto-orchestrator
- platform-update
# Knowledge & Experiments
- knowledge
- experiment-loop
- idle-curiosity
- atlas-team
- atlas-dev-self
- atlas-analytics
- ultrathink
# v7.0 W1 — Observability & Eval foundation
- atlas-trace            # W1.1: OpenTelemetry-like distributed tracing
- skill-scorecard        # W1.2: rolling p50/p99 latency, error rate, cost-per-invocation
- trace-replay           # W1.3: interactive timeline replay for traces
- atlas-eval             # W1.5: LLM-as-judge eval harness for skill regression
- git-guardrails         # W1.7: git safety guardrails companion (cherry-pick mattpocock/skills, MIT)
refs:
# Inherited from dev-addon: composition-patterns, react-best-practices,
# web-design-guidelines, gmining-excel, code-smells-catalog,
# sota-architecture-patterns, naming-conventions, folder-structure-patterns.
- model-benchmarks-2026-04
hooks:
# Inherited from dev-addon (13): post-edit-lint, enterprise-check,
# test-impact-analysis, code-quality-check, frontend-ux-gate, skill-usage-tracker,
# ci-auto-monitor, enforce-worktree-name, cleanup-worktrees, tdd-guard,
# claudemd-lint, detect-stale-git, feature-drift-detector.
- session-end-memory-cleanup
- post-compact
- suggest-episode
- config-change
- instructions-loaded
# v5.7.0+ Phase 6F-bis: Wire pre-v5.7.0 tech-debt hooks (was KNOWN_UNDECLARED_BASELINE)
- tdd-guard               # PreToolUse[Write|Edit] — block edits without failing test
- claudemd-lint           # PostToolUse[Write|Edit] on CLAUDE.md — validate frontmatter + W3H
- detect-stale-git        # Stop — warn if working tree has stale refs / uncommitted
# v7.0 W1 — Observability hooks
- trace-id-injector       # PreToolUse[*] — generate trace+span IDs, write open span (W1.1)
- trace-id-finalizer      # PostToolUse[*] — close span with duration+status (W1.1)
- scorecard-emitter       # PostToolUse[*] — emit per-skill scorecard line (W1.2)
- git-guardrails          # PreToolUse[Bash] — block dangerous git ops (W1.7, mattpocock cherry-pick)
# feature-drift-detector registered in hooks.json SessionStart block (removed from Stop profile 2026-04-18)
agents:
# Inherited from dev-addon (7): plan-architect, plan-reviewer, code-reviewer,
# team-engineer, team-tester, team-researcher, design-implementer.
- context-scanner
- experiment-runner
- team-reviewer
- team-security
- team-coordinator
- data-engineer
- devops-engineer
- domain-analyst
- infra-expert
persona: infrastructure architect
pipeline: DISCOVER → PLAN → STRATEGY → IMPLEMENT → VERIFY → SHIP → DEPLOY → INFRA
banner_label: Admin

[pytest]
markers =
    strict: Strict validation tests (cross-refs, template vars, quality gates). Skip with: -m "not strict"
    smoke: Essential tests (build output, hooks, regression). Run with: -m smoke
    e2e: End-to-end integration tests. Require built dist/ artifacts.
